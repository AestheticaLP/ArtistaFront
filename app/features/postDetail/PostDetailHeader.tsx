import React, { ReactElement } from 'react';
import { Text, View } from 'react-native';

function PostDetailHeader(): ReactElement {
    return (
        <View>
            <Text>Header</Text>
        </View>
    );
}

export default PostDetailHeader;
